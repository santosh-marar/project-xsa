export const dynamic = "force-dynamic";

import OrderManagementComponent from "../_components/custom/order/order";

const OrderPage = () => {
  return <OrderManagementComponent />;
};

export default OrderPage;
